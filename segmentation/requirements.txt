# ==============================================================================
# PyTorch Installation - INSTALL SEPARATELY FIRST!
# ==============================================================================
# DO NOT use "pip install -r requirements.txt" for torch!
# Install torch FIRST with the correct version for your hardware:
#
# For GPU (CUDA 11.8 - recommended):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# For GPU (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# For CPU only (slow, not recommended):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then install other dependencies:
#   pip install -r requirements.txt
#
# Verify installation:
#   python check_installation.py
# ==============================================================================

# torch>=2.0.0  # COMMENTED - Install separately (see above)
# torchvision>=0.15.0  # COMMENTED - Install separately (see above)

# Segmentation models
segmentation-models-pytorch>=0.3.3

# Data processing
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
scikit-learn>=1.3.0
albumentations>=1.3.0

# Visualization & progress
matplotlib>=3.7.0
tqdm>=4.65.0

# Utilities
pyyaml>=6.0
